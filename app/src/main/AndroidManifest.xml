<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.flash"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/jumping_horse_dark_blue"
        android:label="@string/app_name"
        android:roundIcon="@drawable/jumping_horse_dark_blue"
        android:supportsRtl="true"
        android:theme="@style/Theme.HorseRiding"
        tools:targetApi="31">

        <activity
            android:name=".ui.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.LoginPageActivity"
            android:exported="true" />

        <activity
            android:name=".ui.SignUPageActivity"
            android:exported="true" />

        <activity
            android:name=".ui.KYCPageActivity"
            android:exported="true" />
        <activity
            android:name=".ui.HomePageActivity"
            android:exported="true" />
        <activity
            android:name=".ui.ProfileActivity"
            android:exported="true" />
        <activity
            android:name=".ui.ForgetPasswordActivity"
            android:exported="true" />
        <activity
            android:name=".ui.EditProfileActivity"
            android:exported="true" />
        <activity
            android:name=".ui.WalletActivity"
            android:exported="true"/>

    </application>

</manifest>